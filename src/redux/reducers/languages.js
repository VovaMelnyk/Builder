const initialState = [
  {
    language: "Ukrainian",
    level: "native",
  },
];

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
