const initialState = [
  {
    projectTitle: "YouTube",
    company: "Google",
    description: "Bla bla bla bla",
  },
];

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
