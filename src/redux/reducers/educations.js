const initialState = [
  {
    school: "Best school ever",
    degree: "master",
    start: "",
    end: "",
  },
];

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
